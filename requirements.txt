# Core
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0

# Model download
huggingface-hub>=0.20.0

# Computer Vision & Deep Learning (Python 3.12 compatible)
# numpy<2 for compatibility with matplotlib, numba, statsmodels
numpy>=1.26.0,<2
opencv-python>=4.9.0
torch>=2.2.0
torchvision>=0.17.0
ultralytics>=8.0.0

# ANPR
easyocr==1.7.1
imutils==0.5.4
# PaddleOCR (for higher-accuracy text extraction)
paddleocr>=2.7.0
# NOTE: On Windows you also need to install paddlepaddle separately:
# pip install paddlepaddle -f https://www.paddlepaddle.org.cn/whl/windows/mkl/avx/stable.html

# Database - SQLite (built-in), pymongo optional for MongoDB
pymongo==4.6.0
sqlalchemy==2.0.23

# Notifications
twilio==8.10.0
requests>=2.32.2

# Utilities
Pillow==10.1.0
python-dateutil==2.8.2
